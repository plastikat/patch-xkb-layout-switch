# This is a configuration file for
# patch-xkb-layout-switch.sh script

# Target package name as known by the package manager
pkg_name="xserver-xorg-core-hwe-18.04"

# Patch file URL to download it from
patch_url="https://bugs.freedesktop.org/attachment.cgi?id=129861"

# File name to patch, relative to the source code root direcory
target_file_relpath="xkb/xkbActions.c"

# Build direcory name, relative to the script direcory
build_relpath="xorg-server"
